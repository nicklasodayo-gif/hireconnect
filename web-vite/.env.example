## Build-time API base URL used by the web app. Replace with your Render API URL.
VITE_API_BASE_URL=https://YOUR-RENDER-BACKEND.onrender.com

# Do NOT place sensitive secrets here; frontend environment variables are
# embedded into the client bundle at build time and are publicly visible.
